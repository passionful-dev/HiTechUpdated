<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.practice.hitechupdated" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Mobile"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterAccountActivity"
            android:label="@string/title_activity_register_account" >
        </activity>
        <activity
            android:name=".EntryActivityActivity"
            android:label="@string/title_activity_entry" >
        </activity>
        <activity
            android:name=".ReportActivity"
            android:label="@string/title_activity_report" >
        </activity>
        <activity
            android:name=".ReportFromDateActivity"
            android:label="@string/title_activity_report_from_date" >
        </activity>
        <service android:name=".UserRegisteredMessageService"
                 android:exported="false" >
        </service>
    </application>

</manifest>
